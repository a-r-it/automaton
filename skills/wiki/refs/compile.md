# compile

Compile new daily logs and `sources/` entries into typed wiki articles.

```bash
wiki-compile [--all] [--file <path>] [--dry-run]
```

**Args:**
- `--all` — recompile every source, ignore state.
- `--file <path>` — compile a single file.
- `--dry-run` — print planned actions, no writes.
